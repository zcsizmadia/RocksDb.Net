# Results

One file per run. The machine and the pinned RocksDb version are part of
the result: a number without them cannot be compared with anything.

Naming: `<rocksdb-version>-<short-machine-description>.md`.

These are for spotting a change between versions, not for publishing as
absolute throughput — see the note about the in-memory environment in the
project README.
